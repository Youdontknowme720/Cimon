name: CimonPipe

on: [push]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello, World!"

  goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: Faily
        run: edcho "Goodbye, World!"

